//program to demonstrate on functional interface.

package org.tnsif.interfaceexecutor;

public interface ChandrayanThree {
	
	void status();
	
	/* FunctionL interface contains exactly one abstract method
	 * we can use static and default method inside the functional interface*/
}
